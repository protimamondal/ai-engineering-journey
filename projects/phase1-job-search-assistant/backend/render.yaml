services:
  - type: web
    name: protima-job-search-assistant
    runtime: docker
    rootDir: projects/phase1-job-search-assistant/backend
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
